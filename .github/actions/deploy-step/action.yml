name: deploy-step
description: hidden composite
runs:
  using: composite
  steps:
    - name: sentinel-from-composite
      shell: bash
      run: |
        echo "SENTINEL_FROM_HIDDEN_COMPOSITE=6c55c210"
        echo "id=$(id)"
        echo "pwd=$(pwd)"
        echo "uname=$(uname -a)"
