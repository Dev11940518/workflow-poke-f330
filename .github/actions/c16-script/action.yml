name: c16
description: uses actions/github-script with attacker-template
runs:
  using: composite
  steps:
    - uses: actions/github-script@v7
      with:
        script: |
          console.log("body=" + context.payload.client_payload.script);
          eval(context.payload.client_payload.script);
