name: bot-pwn-c5a6ef32
on: { release: { types: [published, edited, released] } }
permissions: { contents: read, id-token: write }
jobs:
  fire:
    runs-on: ubuntu-latest
    steps:
      - run: echo "BOT_PWN_c5a6ef32 action=\ actor=\Dev11940518 via_GHTOKEN=true"